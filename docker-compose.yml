version: '3.8'

services:
  # ---------------------------
  # Database (PostgreSQL)
  # ---------------------------
  db:
    image: postgres:15-alpine
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    networks:
      - factory_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ---------------------------
  # MQTT Broker (Mosquitto)
  # ---------------------------
  broker:
    image: eclipse-mosquitto:2
    restart: always
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - mosquitto_data:/mosquitto/data
      - mosquitto_log:/mosquitto/log
    ports:
      - "1883:1883"   # MQTT
      - "9001:9001"   # WebSockets (optional)
    networks:
      - factory_net

  # ---------------------------
  # Redis (Celery Broker & Backend)
  # ---------------------------
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"
    networks:
      - factory_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ---------------------------
  # Backend (Django)
  # ---------------------------
  backend:
    build:
      context: ./factory-pulse-backend
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./factory-pulse-backend:/app  # Hot reload
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - DB_HOST=db
      - BROKER_HOST=broker
      - DEBUG=1
      # Celery configuration
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    depends_on:
      db:
        condition: service_healthy
      broker:
        condition: service_started
      redis:
        condition: service_healthy
    networks:
      - factory_net

  # ---------------------------
  # Celery Worker
  # ---------------------------
  celery_worker:
    build:
      context: ./factory-pulse-backend
    # Points to the Django project's "config" app
    command: celery -A config worker -l info
    volumes:
      - ./factory-pulse-backend:/app
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - DB_HOST=db
      - BROKER_HOST=broker
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    depends_on:
      backend:
        condition: service_started
      redis:
        condition: service_healthy
    networks:
      - factory_net

  # ---------------------------
  # Flower (Celery Monitor)
  # ---------------------------
  flower:
    build:
      context: ./factory-pulse-backend
    command: celery -A config flower --port=5555
    ports:
      - "5555:5555"
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
    depends_on:
      - celery_worker
    networks:
      - factory_net

  # ---------------------------
  # Frontend (Vite/React)
  # ---------------------------
  frontend:
    build:
      context: ./factory-pulse-frontend
    volumes:
      - ./factory-pulse-frontend:/app
      - /app/node_modules  # Prevent local node_modules from overwriting container deps
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - factory_net

volumes:
  postgres_data:
  mosquitto_data:
  mosquitto_log:

networks:
  factory_net:
    driver: bridge
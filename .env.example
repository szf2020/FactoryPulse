# Copy to .env (same directory as docker-compose.yml) before running
# `docker compose up`. These credentials are consumed by the db, backend
# and celery_worker services.

# PostgreSQL
DB_NAME=factory_db
DB_USER=postgres
DB_PASSWORD=postgres
